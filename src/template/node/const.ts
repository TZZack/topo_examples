import {checkValid} from '../util/index.ts'

const testValid = () => {
  return checkValid()
}
testValid()

export const data = {
  nodes: [
    {
      id: 'circle',
      label: 'Circle',
      x: 250,
      y: 150,
    },
  ],
};